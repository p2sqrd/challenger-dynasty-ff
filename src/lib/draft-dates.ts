/**
 * Key 2026 dates for the Draft page countdowns. Both are TBD until the
 * commissioner sets them — leave a value `null` to show "TBD" on the page,
 * or drop in an ISO 8601 string *with a timezone offset* (Eastern shown
 * here) once the date is locked, e.g. "2026-08-15T23:59:00-04:00".
 */
export const KEEPER_DEADLINE: string | null = null;
export const DRAFT_DATETIME: string | null = null;
